function analysisCtrl($scope, $filter, $http, $location, $route, $q, $log, $rootScope, $routeParams, $timeout, $translate, dialogs,
                       resolvedCategoriesTotals
                       /*,
                       resolvedSubCategoriesTotals,
                       resolvedMonthAccrual,
                       resolvedVendorsTotals*/
                      ) {

    console.log(resolvedCategoriesTotals);
    $scope.categoryTotals = resolvedCategoriesTotals;
    console.log(resolvedCategoriesTotals);
    
    /*$scope.subCategoryTotals = resolvedCategoriesTotals;
    
    $scope.monthAccrual = resolvedCategoriesTotals;

    $scope.vendorsTotals = resolvedCategoriesTotals;*/
    





















}